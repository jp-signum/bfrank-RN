import React from 'react'

import { withContext } from '../../AppContext'

function Account() {
    
    return (
        <div>
            testttt
        </div>
    )
}

export default withContext(Account);